export type Delegation = {
    delegator: string;
    reducedDelegator: string;
    from: string;
    reducedFrom: string;
    blockNumber: number;
    txnHash: string;
    reducedTxnHash: string;
    amount: number;
};
